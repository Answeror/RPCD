#include "wheel_disabled_scrollarea.hpp"

void cvcourse::wheel_disabled_scrollarea::wheelEvent(QWheelEvent *e)
{
    //if (isHidden()) {
    //    base_type::wheelEvent(e);
    //} else {
    //    hide();
    //    base_type::wheelEvent(e);
    //    show();
    //}
}

